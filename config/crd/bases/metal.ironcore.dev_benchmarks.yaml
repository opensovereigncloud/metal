---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: benchmarks.metal.ironcore.dev
spec:
  group: metal.ironcore.dev
  names:
    kind: Benchmark
    listKind: BenchmarkList
    plural: benchmarks
    singular: benchmark
  scope: Namespaced
  versions:
  - name: v1alpha4
    schema:
      openAPIV3Schema:
        description: Benchmark is the Schema for the machines API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: BenchmarkSpec contains machine benchmark results.
            properties:
              benchmarks:
                additionalProperties:
                  items:
                    properties:
                      name:
                        description: Name is the specific benchmark name. e.g. `fio-1k`.
                        type: string
                      value:
                        description: Value is the exact result of specific benchmark.
                        format: int64
                        type: integer
                    required:
                    - name
                    type: object
                  type: array
                description: Benchmarks is the collection of benchmarks.
                type: object
            type: object
          status:
            description: BenchmarkStatus contains machine benchmarks deviations.
            properties:
              machine_deviation:
                additionalProperties:
                  items:
                    description: BenchmarkDeviation is a deviation between old value
                      and the new one.
                    properties:
                      name:
                        description: Name is the specific benchmark name. e.g. `fio-1k`.
                        type: string
                      value:
                        description: Value is the exact result of specific benchmark.
                        type: string
                    required:
                    - name
                    type: object
                  type: array
                description: BenchmarkDeviations shows the difference between last
                  and current benchmark results.
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
