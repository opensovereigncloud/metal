---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: machineassignments.machine.onmetal.de
spec:
  group: machine.onmetal.de
  names:
    kind: MachineAssignment
    listKind: MachineAssignmentList
    plural: machineassignments
    singular: machineassignment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: Order Status
      type: string
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: MachineAssignment is the Schema for the requests API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: MachineAssignmentSpec defines the desired state of Request.
            properties:
              efiVars:
                description: EFIVars are variables to pass to EFI while booting up.
                items:
                  description: EFIVar is a variable to pass to EFI while booting up.
                  properties:
                    name:
                      type: string
                    uuid:
                      type: string
                    value:
                      type: string
                  required:
                  - value
                  type: object
                type: array
              ignition:
                description: Ignition is a reference to a config map containing the
                  ignition YAML for the machine to boot up. If key is empty, DefaultIgnitionKey
                  will be used as fallback.
                properties:
                  key:
                    description: The key of the entry in the ConfigMap resource's
                      `data` field to be used. Some instances of this field may be
                      defaulted, in others it may be required.
                    type: string
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
              image:
                description: Image is the URL providing the operating system image
                  of the machine.
                type: string
              machineClass:
                description: MachineClass is a reference to the machine class/flavor
                  of the machine.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
              networkInterfaces:
                description: Interfaces define a list of network interfaces present
                  on the machine
                items:
                  description: Interface is the definition of a single interface.
                  properties:
                    ip:
                      description: IP specifies a concrete IP address which should
                        be allocated from a Subnet
                      type: string
                    name:
                      description: Name is the name of the interface
                      type: string
                    priority:
                      description: Priority is the priority level of this interface
                      format: int32
                      type: integer
                    target:
                      description: Target is the referenced resource of this interface.
                      properties:
                        name:
                          description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            TODO: Add other useful fields. apiVersion, kind, uid?'
                          type: string
                      type: object
                  required:
                  - name
                  - target
                  type: object
                type: array
              tolerations:
                description: Tolerations define tolerations the Machine has. Only
                  MachinePools whose taints covered by Tolerations will be considered
                  to run the Machine.
                items:
                  description: The resource this Toleration is attached to tolerates
                    any taint that matches the triple <key,value,effect> using the
                    matching operator <operator>.
                  properties:
                    effect:
                      description: Effect indicates the taint effect to match. Empty
                        means match all taint effects. When specified, allowed values
                        are NoSchedule.
                      type: string
                    key:
                      description: Key is the taint key that the toleration applies
                        to. Empty means match all taint keys. If the key is empty,
                        operator must be Exists; this combination means to match all
                        values and all keys.
                      type: string
                    operator:
                      description: Operator represents a key's relationship to the
                        value. Valid operators are Exists and Equal. Defaults to Equal.
                        Exist is equivalent to wildcard for value, so that a resource
                        can tolerate all taints of a particular category.
                      type: string
                    value:
                      description: Value is the taint value the toleration matches
                        to. If the operator is Exists, the value should be empty,
                        otherwise just a regular string.
                      type: string
                  type: object
                type: array
              volumes:
                description: Volumes are volumes attached to this machine.
                items:
                  description: Volume defines a volume attachment of a machine.
                  properties:
                    name:
                      description: Name is the name of the VolumeAttachment
                      type: string
                    priority:
                      description: Priority is the OS priority of the volume.
                      format: int32
                      type: integer
                    volumeClaim:
                      description: VolumeClaim instructs the VolumeAttachment to use
                        a VolumeClaim as source for the attachment.
                      properties:
                        ref:
                          description: Ref is a reference to the VolumeClaim.
                          properties:
                            name:
                              description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                TODO: Add other useful fields. apiVersion, kind, uid?'
                              type: string
                          type: object
                      required:
                      - ref
                      type: object
                  required:
                  - name
                  type: object
                type: array
            required:
            - image
            - machineClass
            type: object
          status:
            description: MachineAssignmentStatus defines the observed state of Request.
            properties:
              computeMachineRef:
                description: ResourceReference defines related resource info.
                properties:
                  apiVersion:
                    description: APIVersion refers to the resource API version
                    type: string
                  kind:
                    description: Kind refers to the resource kind
                    type: string
                  name:
                    description: Name refers to the resource name
                    type: string
                  namespace:
                    description: Namespace refers to the resource namespace
                    type: string
                type: object
              machineRef:
                description: ResourceReference defines related resource info.
                properties:
                  apiVersion:
                    description: APIVersion refers to the resource API version
                    type: string
                  kind:
                    description: Kind refers to the resource kind
                    type: string
                  name:
                    description: Name refers to the resource name
                    type: string
                  namespace:
                    description: Namespace refers to the resource namespace
                    type: string
                type: object
              state:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
