---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: requests.machine.onmetal.de
spec:
  group: machine.onmetal.de
  names:
    kind: Request
    listKind: RequestList
    plural: requests
    singular: request
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Request is the Schema for the requests API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RequestSpec defines the desired state of Request
            properties:
              efiVars:
                description: EFIVars are variables to pass to EFI while booting up.
                items:
                  description: EFIVar is a variable to pass to EFI while booting up.
                  properties:
                    name:
                      type: string
                    uuid:
                      type: string
                    value:
                      type: string
                  required:
                  - value
                  type: object
                type: array
              hostname:
                description: Hostname is the hostname of the machine
                type: string
              ignition:
                description: Ignition is a reference to a config map containing the
                  ignition YAML for the machine to boot up. If key is empty, DefaultIgnitionKey
                  will be used as fallback.
                properties:
                  key:
                    description: The key of the entry in the ConfigMap resource's
                      `data` field to be used. Some instances of this field may be
                      defaulted, in others it may be required.
                    type: string
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
              image:
                description: Image is the URL providing the operating system image
                  of the machine.
                type: string
              interfaces:
                description: Interfaces define a list of network interfaces present
                  on the machine
                items:
                  description: Interface is the definition of a single interface
                  properties:
                    ip:
                      description: IP specifies a concrete IP address which should
                        be allocated from a Subnet
                      type: string
                    name:
                      description: Name is the name of the interface
                      type: string
                    priority:
                      description: Priority is the priority level of this interface
                      format: int32
                      type: integer
                    target:
                      description: Target is the referenced resource of this interface.
                      properties:
                        name:
                          description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            TODO: Add other useful fields. apiVersion, kind, uid?'
                          type: string
                      type: object
                  required:
                  - name
                  - target
                  type: object
                type: array
              kind:
                default: Machine
                description: Kind defines request server type. Machine or Switch or
                  Router.
                type: string
              machineClass:
                description: MachineClass is a reference to the machine class/flavor
                  of the machine.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
              machinePool:
                description: MachinePool defines machine pool to run the machine in.
                  If empty, a scheduler will figure out an appropriate pool to run
                  the machine in.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
              machinePoolSelector:
                additionalProperties:
                  type: string
                description: MachinePoolSelector selects a suitable MachinePool by
                  the given labels.
                type: object
              securityGroups:
                description: SecurityGroups is a list of security groups of a machine
                items:
                  description: LocalObjectReference contains enough information to
                    let you locate the referenced object inside the same namespace.
                  properties:
                    name:
                      description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        TODO: Add other useful fields. apiVersion, kind, uid?'
                      type: string
                  type: object
                type: array
              sshPublicKeys:
                description: SSHPublicKeys is a list of SSH public key secret references
                  of a machine.
                items:
                  description: ObjectSelector is a reference to a specific 'key' within
                    a ConfigMap resource. In some instances, `key` is a required field.
                  properties:
                    key:
                      description: The key of the entry in the ConfigMap resource's
                        `data` field to be used. Some instances of this field may
                        be defaulted, in others it may be required.
                      type: string
                    name:
                      description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        TODO: Add other useful fields. apiVersion, kind, uid?'
                      type: string
                  type: object
                type: array
              tolerations:
                description: Tolerations define tolerations the Machine has. Only
                  MachinePools whose taints covered by Tolerations will be considered
                  to run the Machine.
                items:
                  description: The resource this Toleration is attached to tolerates
                    any taint that matches the triple <key,value,effect> using the
                    matching operator <operator>.
                  properties:
                    effect:
                      description: Effect indicates the taint effect to match. Empty
                        means match all taint effects. When specified, allowed values
                        are NoSchedule.
                      type: string
                    key:
                      description: Key is the taint key that the toleration applies
                        to. Empty means match all taint keys. If the key is empty,
                        operator must be Exists; this combination means to match all
                        values and all keys.
                      type: string
                    operator:
                      description: Operator represents a key's relationship to the
                        value. Valid operators are Exists and Equal. Defaults to Equal.
                        Exists is equivalent to wildcard for value, so that a resource
                        can tolerate all taints of a particular category.
                      type: string
                    value:
                      description: Value is the taint value the toleration matches
                        to. If the operator is Exists, the value should be empty,
                        otherwise just a regular string.
                      type: string
                  type: object
                type: array
              volumeAttachments:
                description: VolumeAttachments are volumes attached to this machine.
                items:
                  description: VolumeAttachment defines a volume attachment of a machine
                  properties:
                    name:
                      description: Name is the name of the VolumeAttachment
                      type: string
                    priority:
                      description: Priority is the OS priority of the volume.
                      format: int32
                      type: integer
                    volumeClaim:
                      description: VolumeClaim instructs the VolumeAttachment to use
                        a VolumeClaim as source for the attachment.
                      properties:
                        ref:
                          description: Ref is a reference to the VolumeClaim.
                          properties:
                            name:
                              description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                TODO: Add other useful fields. apiVersion, kind, uid?'
                              type: string
                          type: object
                      required:
                      - ref
                      type: object
                  required:
                  - name
                  type: object
                type: array
            required:
            - hostname
            - image
            - machineClass
            type: object
          status:
            description: RequestStatus defines the observed state of Request
            properties:
              reference:
                description: ResourceReference defines related resource info
                properties:
                  apiVersion:
                    description: APIVersion refers to the resource API version
                    type: string
                  kind:
                    description: Kind refers to the resource kind
                    type: string
                  name:
                    description: Name refers to the resource name
                    type: string
                  namespace:
                    description: Namespace refers to the resource namespace
                    type: string
                type: object
              state:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
