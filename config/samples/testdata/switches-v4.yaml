apiVersion: ipam.onmetal.de/v1alpha1
kind: Subnet
metadata:
  name: switches-v4
  namespace: onmetal
spec:
  capacity: 255
  parentSubnet:
    name: switch-ranges-v4
  network:
    name: underlay
  regions:
    - name: eu-west
      availabilityZones:
        - A